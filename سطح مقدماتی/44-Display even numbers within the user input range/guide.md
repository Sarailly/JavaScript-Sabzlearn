1:برای مشخص کردن عدد کوچک و بزرگ 
let minNumber=Math.min(firstNumber, secondNumber);
let maxNumber=Math.max(firstNumber, secondNumber);

2:در صورتی که عدد فرد باشه یک واحد بهش اضافه شه تا زوج یشه
 minNumber++